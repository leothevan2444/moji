enum TaskEventType {
  CREATED
  UPDATED
  DELETED
}

type TaskEvent {
  sequence: Int!
  type: TaskEventType!
  taskId: ID!
  task: Task
  dashboardStats: DashboardStats!
}

type Subscription {
  taskEvents: TaskEvent!
}
