query DashboardDocument {
  health {
    ok
    message
  }
  version
  dashboardStats {
    total
    active
    completed
    downloading
    pendingScans
    failed
  }
  settings {
    stash {
      configured
      enabled
      url
      apiKeyConfigured
      apiKey
      mode
      libraryPath
      qbittorrentPathPrefix
      stashPathPrefix
      transferAction
      transferTargetPath
    }
    jackett {
      configured
      enabled
      url
      apiKeyConfigured
      apiKey
    }
    qbittorrent {
      configured
      enabled
      url
      username
      usernameConfigured
      passwordConfigured
      password
      defaultSavePath
      category
      tags
    }
    tasks {
      store
      dbPath
      progressSyncIntervalSeconds
      progressSyncEnabled
    }
    subscription {
      store
      dbPath
      pollIntervalSeconds
      pollEnabled
      stashBoxes {
        name
        endpoint
        apiKeyConfigured
      }
      stashBoxEndpoints
      stashBoxesLoaded
      stashBoxesLoadError
    }
    logging {
      level
      filePath
      maxEntries
      maxFileSizeBytes
      maxFileBackups
    }
    system {
      appVersion
    }
  }
  tasks {
    id
    query
    status
    torrentName
    progress
    qbittorrentState
    contentPath
    torrentHash
    savePath
    category
    tags
    error
    completedAt
    stashMode
    stashSourcePath
    stashTransferAction
    stashTransferPath
    stashTransferStatus
    stashTransferError
    stashJobId
    stashScanPath
    stashScanStatus
    stashScanError
    stashScanHint
    createdAt
    updatedAt
  }
}
