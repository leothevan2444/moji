query DashboardDocument {
  health {
    ok
    message
  }
  version
  dashboardStats {
    total
    active
    completed
    downloading
    pendingScans
    failed
  }
  settings {
    stash {
      configured
      enabled
      url
      apiKeyConfigured
      apiKey
      libraryPath
    }
    jackett {
      configured
      enabled
      url
      apiKeyConfigured
      apiKey
    }
    qbittorrent {
      configured
      enabled
      url
      username
      usernameConfigured
      passwordConfigured
      password
      defaultSavePath
      category
      tags
    }
    tasks {
      store
      dbPath
      progressSyncIntervalSeconds
      progressSyncEnabled
    }
    subscription {
      store
      dbPath
      pollIntervalSeconds
      pollEnabled
      stashBoxes {
        name
        endpoint
        apiKeyConfigured
      }
      stashBoxEndpoints
      stashBoxesLoaded
      stashBoxesLoadError
    }
    logging {
      level
      filePath
      maxEntries
      maxFileSizeBytes
      maxFileBackups
    }
    system {
      appVersion
    }
  }
  tasks {
    id
    query
    status
    torrentName
    progress
    qbittorrentState
    contentPath
    torrentHash
    savePath
    category
    tags
    error
    completedAt
    stashJobId
    stashScanStatus
    stashScanError
    createdAt
    updatedAt
  }
}
