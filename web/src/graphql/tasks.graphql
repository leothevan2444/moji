mutation AddTorrentDocument($input: QBittorrentAddInput!) {
  addTorrent(input: $input) {
    id
    source
    stage
    stageStatus
    stageLabel
    stageStatusLabel
    stageErrorCode
    stageErrorMessage
    code
    torrentName
    progress
    deliveryMode
    createdAt
  }
}

mutation SyncTaskProgressDocument {
  syncTaskProgress {
    id
    source
    stage
    stageStatus
    stageLabel
    stageStatusLabel
    progress
    qbittorrentState
    updatedAt
  }
}

mutation TriggerStashScansDocument {
  triggerStashScans {
    id
    source
    deliveryMode
    transferError
    stashScanJobId
    stashScanPath
    stashScanError
    stashScanHint
    updatedAt
  }
}

mutation TriggerTaskStashScanDocument($id: ID!) {
  triggerTaskStashScan(id: $id) {
    id
    source
    deliveryMode
    transferError
    stashScanJobId
    stashScanPath
    stashScanError
    stashScanHint
    updatedAt
  }
}

mutation DeleteTaskDocument($id: ID!) {
  deleteTask(id: $id) {
    id
    source
    stage
    stageStatus
    stageLabel
    stageStatusLabel
    code
    updatedAt
  }
}

mutation RetryTaskDocument($id: ID!) {
  retryTask(id: $id) {
    id
    source
    stage
    stageStatus
    stageLabel
    stageStatusLabel
    stageErrorCode
    stageErrorMessage
    code
    progress
    qbittorrentState
    updatedAt
  }
}
